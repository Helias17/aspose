export type AuthData = {
  email: string;
  password: string;
};

export type CityWeather = {
  location: string;
  temp: number;
  id: number;
};

export type FormDataType = {
  grade: string;
  text: string;
  email: string;
  page?: string;
};
